#include "../../include/src_include/Core/ressources_manager.h"


void *_create_allocation(void *data, size_t size_data)
{
    data = malloc(sizeof(size_data));
    if(!data)
    {

    }
    return NULL;
}